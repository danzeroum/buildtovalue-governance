# BuildToValue v7.3 - Environment Variables Template
# Copy to .env and update values

# === ENVIRONMENT ===
ENVIRONMENT=development
LOG_LEVEL=INFO

# === SECRETS (Generate with: ./scripts/rotate_secrets.sh) ===
JWT_SECRET=your-jwt-secret-here-256-bits
HMAC_KEY=your-hmac-key-here-256-bits

# === DATABASE ===
DB_URL=postgresql://btv:password@localhost:5432/btv_governance
# OR for SQLite (development only):
# DB_URL=sqlite:///./data/btv_registry.db

# === API CONFIGURATION ===
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4

# === CORS (comma-separated origins) ===
CORS_ORIGINS=http://localhost:3000,https://app.buildtovalue.com

# === RATE LIMITING ===
RATE_LIMIT_PER_MINUTE=100

# === COMPLIANCE ===
DEFAULT_JURISDICTION=EU
ENABLE_EU_DATABASE_SYNC=false

# === OPTIONAL: External Services ===
# DATADOG_API_KEY=your-datadog-key
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
# SENTRY_DSN=https://your-sentry-dsn
